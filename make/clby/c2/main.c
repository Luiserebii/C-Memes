/**
 * Note the usage of <>s below. Compiling with the -I flag, and signaling
 * the current directory, will allow this compile to go through.
 */

#include <hellomake.h>

int main() {
    hellomake();
    return 0;
}
